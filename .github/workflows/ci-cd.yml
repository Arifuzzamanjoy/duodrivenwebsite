name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov flake8
    
    - name: Lint with flake8
      run: |
        # Stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      continue-on-error: true
    
    - name: Run tests
      run: |
        # Create a simple test if none exist
        if [ ! -d "tests" ]; then
          echo "No tests directory found, skipping tests"
        else
          pytest tests/ --cov=. --cov-report=xml --cov-report=html
        fi
      continue-on-error: true
    
    - name: Check Flask app syntax
      run: |
        python -m py_compile app.py config.py

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Build application
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        echo "Application built successfully"
    
    - name: Create deployment artifact
      run: |
        mkdir -p artifacts
        tar -czf artifacts/duodriven-app.tar.gz \
          --exclude='.git' \
          --exclude='.github' \
          --exclude='__pycache__' \
          --exclude='*.pyc' \
          --exclude='venv' \
          --exclude='.venv' \
          --exclude='artifacts' \
          .
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: duodriven-app
        path: artifacts/duodriven-app.tar.gz
        retention-days: 30

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: duodriven-app
    
    - name: Deploy notification
      run: |
        echo "Deployment step - Configure your deployment target here"
        echo "Examples: Deploy to Heroku, AWS, DigitalOcean, etc."
        echo "Artifact ready: duodriven-app.tar.gz"
    
    # Uncomment and configure based on your deployment target
    # - name: Deploy to Server
    #   run: |
    #     # Add your deployment commands here
    #     # scp, rsync, or cloud provider CLI commands
